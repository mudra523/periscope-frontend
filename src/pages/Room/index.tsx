import React from "react";
import Layout from "../../layout/index";

type Props = {};

const Home = (props: Props) => {
  return (
    <Layout>
      <>{console.log("Home")}</>
      Home
    </Layout>
  );
};

export default Home;
